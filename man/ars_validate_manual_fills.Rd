% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ars_to_ard.R
\name{ars_validate_manual_fills}
\alias{ars_validate_manual_fills}
\title{Validate manually-filled ARD cells}
\usage{
ars_validate_manual_fills(ard)
}
\arguments{
\item{ard}{An ARD data frame (class \code{"card"}), typically one whose
\code{manual_pending} cells (see \code{\link[=ars_manual_worklist]{ars_manual_worklist()}}) have been filled.}
}
\value{
A data frame, one row per offending cell: \code{output_id},
\code{analysis_id}, \code{method_id}, \code{stat_name}, and \code{problem}. Zero rows (with
those columns) when every manual fill is traceable.
}
\description{
Checks the manual fills in an ARD (ADR 0002 phase 5). A cell whose
\code{result_status} was set to \code{"manual_filled"} must carry both a value
(\code{stat}) and a \code{derivation_ref} -- the path/id of the validated program that
produced it. A manual value with no derivation reference is untraceable and
must never ship; \code{\link[=ars_render_all]{ars_render_all()}} surfaces any offending row as a blocker
diagnostic before rendering. Run it yourself on a filled ARD to clear the
worklist.
}
\examples{
\dontrun{
  bad <- ars_validate_manual_fills(filled_ard)
  if (nrow(bad)) stop("untraceable manual values present")
}
}
\seealso{
\code{\link[=ars_manual_worklist]{ars_manual_worklist()}}
}
