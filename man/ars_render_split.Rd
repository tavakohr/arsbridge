% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ars_render_docx.R
\name{ars_render_split}
\alias{ars_render_split}
\title{Render each output to its own ARD and table file}
\usage{
ars_render_split(
  ars_path,
  dir,
  adam_dir = NULL,
  ard = NULL,
  output_ids = NULL,
  format = c("rtf", "docx"),
  write_ard = TRUE,
  max_rows = 500
)
}
\arguments{
\item{ars_path}{Path to the ARS reporting-event JSON.}

\item{dir}{Output directory. Created (recursively) if it does not exist.}

\item{adam_dir}{Directory of ADaM datasets. Required to compute the ARD when
\code{ard} is \code{NULL}, and to render listings/figures.}

\item{ard}{Optional precomputed ARD (from \code{\link[=ars_to_ard]{ars_to_ard()}}). When \code{NULL} it is
computed from \code{ars_path} + \code{adam_dir}.}

\item{output_ids}{Optional character vector restricting which outputs to
render (matched against output id or name, case-insensitively).}

\item{format}{Table file format: \code{"rtf"} (default) or \code{"docx"}.}

\item{write_ard}{Also write each output's ARD slice as
\verb{<dir>/<output_id>.rds}. Default \code{TRUE}.}

\item{max_rows}{Row cap for listings. Default 500.}
}
\value{
Invisibly, a manifest data frame: \code{output_id}, \code{type}, \code{status}
(\code{"rendered"}/\code{"error"}/\code{"skipped"}), \code{ard_file}, \code{doc_file}, \code{reason}.
\code{attr(., "dir")} is the output directory.
}
\description{
The "one file per program" companion to \code{\link[=ars_render_all]{ars_render_all()}}: instead of a
single combined document, this writes a separate table file (and, by
default, a separate ARD \code{.rds}) for every output in the reporting event --
the layout most clinical repositories expect, one deliverable per TLF
program.
}
\details{
Tables and listings are written with the same regulatory flextable styling
as \code{\link[=ars_render_all]{ars_render_all()}}; figures are written as \code{.png}. Each output's ARD
slice (its rows of the big ARD, cards list-columns intact) is saved as
\verb{<dir>/<output_id>.rds} unless \code{write_ard = FALSE}.
}
\seealso{
\code{\link[=ars_render_combined]{ars_render_combined()}} for one big ARD + one combined RTF,
\code{\link[=ars_render_all]{ars_render_all()}} for a single combined Word document.
}
