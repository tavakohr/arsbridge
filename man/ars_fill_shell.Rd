% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ars_fill_shell.R
\name{ars_fill_shell}
\alias{ars_fill_shell}
\title{Write results into the shell workbook they came from}
\usage{
ars_fill_shell(
  shell_path,
  ars,
  ard,
  output_path,
  adam_dir = NULL,
  strip_annotations = TRUE,
  keep_pending_placeholders = TRUE,
  overwrite = FALSE
)
}
\arguments{
\item{shell_path}{Path to the Excel shell (\code{.xlsx}) the ARS was built from.}

\item{ars}{The reporting event: a path to \code{reporting_event.json}, or the
parsed list.}

\item{ard}{The ARD from \code{\link[=ars_to_ard]{ars_to_ard()}} holding the results to write.}

\item{output_path}{Where to write the filled workbook.}

\item{adam_dir}{Directory holding the ADaM datasets (\code{.xpt}, \code{.sas7bdat}
or \code{.csv}). Required to fill a listing, whose rows are the subject-level
data itself, and a figure, whose series the shell states as prose rather
than as an analysis. Tables need only the ARD; leave it \code{NULL} and any
listing or figure sheet is reported instead of filled.}

\item{strip_annotations}{Remove the red programming annotations. \code{TRUE} for
a deliverable; \code{FALSE} to keep them beside the numbers while reviewing.}

\item{keep_pending_placeholders}{Leave an unfillable cell showing its
placeholder (default) rather than blanking it.}

\item{overwrite}{Allow \code{output_path} to be replaced.}
}
\value{
Invisibly, a list with \code{path}, the counts \code{filled}, \code{pending} and
\code{skipped}, and \code{diagnostics} -- one row per cell that was not filled,
with the output, the cell reference and the reason.
}
\description{
Takes the Excel shell that was parsed to build an ARS, and returns a copy
of it with the computed results written into their placeholders and the
programming annotations removed. The layout, labels, column headers,
merges, fonts and footnotes are the author's own -- they are never
rebuilt, only left alone.
}
\details{
Which result belongs in which cell is not decided here. It was recorded
when the ARS was built, in each output's \verb{_meta$shell_fill} cell map (see
\code{vignette("shell-fidelity")} and ADR 0005), so a shell that was parsed by
an older version, or a Word shell, has no map and nothing to fill.

A cell whose result does not exist keeps its placeholder and is reported as
pending rather than blanked, because an empty cell in a clinical table
reads as a zero. \code{keep_pending_placeholders = FALSE} clears them instead,
for a workbook that is going to someone who will complete it by hand.
}
\examples{
\dontrun{
  ard <- ars_to_ard("outputs/reporting_event.json", "inputs/ADaM")
  ars_fill_shell(
    shell_path  = "inputs/shells.xlsx",
    ars         = "outputs/reporting_event.json",
    ard         = ard,
    output_path = "outputs/filled_shells.xlsx")
}
}
\seealso{
\code{\link[=ars_to_ard]{ars_to_ard()}} for the results, \code{\link[=spec_to_ars]{spec_to_ars()}} for the map.
}
