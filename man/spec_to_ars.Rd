% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec_to_ars.R
\name{spec_to_ars}
\alias{spec_to_ars}
\title{Convert annotated TLF shell and ADaM spec to CDISC ARS JSON}
\usage{
spec_to_ars(
  shell_path,
  adam_spec_path,
  output_path = "reporting_event.json",
  study_id = "STUDY-001",
  study_name = NULL,
  model = "claude-sonnet-4-6",
  api_key = Sys.getenv("ANTHROPIC_API_KEY"),
  validate = TRUE,
  report_path = "spec_validation_report.xlsx",
  verbose = TRUE
)
}
\arguments{
\item{shell_path}{Path to annotated TLF shells \code{.docx}.}

\item{adam_spec_path}{Path to the ADaM specification. Accepts either:
\itemize{
\item \code{.xml} -- ADaM \code{define.xml} (preferred when available)
\item \code{.xlsx} / \code{.xls} -- ADaM specification Excel (fallback used during
development before \code{define.xml} is produced)
}

One of the two is required. The SDTM spec is NOT a valid input --
TLF annotations reference ADaM variables, so the grounding source
must be the ADaM spec.}

\item{output_path}{Path for the ARS JSON. Default \code{"reporting_event.json"}.}

\item{study_id}{Study identifier. Default \code{"STUDY-001"}.}

\item{study_name}{Human-readable study name. Defaults to \code{study_id}.}

\item{model}{Anthropic model. Default \code{"claude-sonnet-4-6"}.}

\item{api_key}{Anthropic API key. Defaults to env \code{ANTHROPIC_API_KEY}.}

\item{validate}{If \code{TRUE} (default), cross-reference annotations
against the ADaM spec and write a validation report.}

\item{report_path}{Path for the validation report \code{.xlsx}.
Default \code{"spec_validation_report.xlsx"}.}

\item{verbose}{Print progress messages. Default \code{TRUE}.}
}
\value{
Invisibly returns a named list:
\describe{
\item{\code{ars_path}}{Path to the generated ARS JSON file.}
\item{\code{report_path}}{Path to the validation report (if validate=TRUE).}
\item{\code{n_tlfs}}{Number of TLF sections processed.}
\item{\code{n_analyses}}{Number of ARS Analysis objects created.}
\item{\code{n_warnings}}{Number of spec validation warnings.}
\item{\code{reporting_event}}{The full ARS ReportingEvent as a nested R
list -- the same content that was serialised to \code{ars_path}. Inspect
interactively with e.g. \code{str(res$reporting_event, max.level = 2)}.}
\item{\code{validation}}{Data frame of per-annotation validation results
(\code{tlf_number}, \code{stub_label}, \code{annotation}, \code{variable_ref}, \code{status},
\code{message}). \code{NULL} when \code{validate = FALSE}.}
}
}
\description{
Reads a lead programmer's already-annotated TLF shells Word document and
the study's ADaM specification Excel, and produces a valid CDISC Analysis
Results Standard (ARS) v1.0 ARM-TS JSON file consumable by
\code{siera::readARS()}.
}
\section{Human review}{

The generated ARS JSON is a draft. A qualified clinical programmer MUST
review it before downstream use. The JSON includes a
\verb{_meta.requires_human_review = TRUE} field that consumers can key on.
}

\examples{
\dontrun{
spec_to_ars(
  shell_path     = "inputs/annotated_shells.docx",
  adam_spec_path = "inputs/adam_spec.xlsx",
  output_path    = "outputs/reporting_event.json",
  report_path    = "outputs/spec_validation_report.xlsx"
)
}
}
