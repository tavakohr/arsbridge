% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec_to_ars.R
\name{spec_to_ars}
\alias{spec_to_ars}
\title{Convert annotated TLF shell and ADaM spec to CDISC ARS JSON}
\usage{
spec_to_ars(
  shell_path,
  adam_spec_path,
  sap_path = NULL,
  output_path = file.path(tempdir(), "reporting_event.json"),
  study_id = "STUDY-001",
  study_name = NULL,
  model = NULL,
  api_key = NULL,
  provider = NULL,
  supplement = NULL,
  use_llm = FALSE,
  spec_column_aliases = NULL,
  extract_with_llm = TRUE,
  ship_annotations = FALSE,
  heading_patterns = NULL,
  validate = TRUE,
  report_path = file.path(tempdir(), "spec_validation_report.xlsx"),
  code_dir = NULL,
  adam_dir = ".",
  verbose = TRUE
)
}
\arguments{
\item{shell_path}{Path to annotated TLF shells \code{.docx}.}

\item{adam_spec_path}{Path to the ADaM specification. Accepts either:
\itemize{
\item \code{.xml} -- ADaM \code{define.xml} (preferred when available)
\item \code{.xlsx} / \code{.xls} -- ADaM specification Excel (fallback used during
development before \code{define.xml} is produced)
}

One of the two is required. The SDTM spec is NOT a valid input --
TLF annotations reference ADaM variables, so the grounding source
must be the ADaM spec.}

\item{sap_path}{Optional path to the Statistical Analysis Plan \code{.docx}.
When supplied, its prose is matched per TLF and carried into each analysis
as \code{sapDescription}, becoming the human-readable comment above the emitted
\code{{cards}} block. Gracefully ignored when absent or unreadable.}

\item{output_path}{Path for the ARS JSON. Defaults to
\code{reporting_event.json} in \code{\link[=tempdir]{tempdir()}}; pass an explicit path to write it
somewhere permanent.}

\item{study_id}{Study identifier. Default \code{"STUDY-001"}.}

\item{study_name}{Human-readable study name. Defaults to \code{study_id}.}

\item{model}{LLM model. Defaults to the active provider's default model.}

\item{api_key}{LLM API key. Defaults to the active provider's key.}

\item{provider}{LLM provider: \code{"anthropic"}, \code{"openai"}, or \code{"gemini"}.
Defaults to the active provider.}

\item{supplement}{Optional path to a supplement \code{.json} produced by a
chat assistant from the instruction file written by
\code{\link[=ars_copilot_instructions]{ars_copilot_instructions()}}. When supplied, NO live LLM calls are made
(even if a key is set): the supplement's bindings fill only the rows the
deterministic pass left unannotated (shell annotations always win;
disagreements are WARN findings) and its per-TLF fields feed the same
enrichment path a live LLM answer would. Every supplement variable
passes the hard ADaM-spec gate. Pre-flight a file with
\code{\link[=ars_validate_supplement]{ars_validate_supplement()}}.

Regex is the always-on baseline and the default; the LLM is opt-in (see
\code{use_llm}). Deterministic and supplement are first-class modes -- the
function never asks for a key nor raises a key-related error or warning in
them; the mode that ran is recorded as a neutral INFO note and in
\code{extraction_mode} / \verb{_meta.extraction_mode}.}

\item{use_llm}{Opt in to the live LLM tier. Default \code{FALSE} -- the pipeline
runs regex-only (deterministic) and makes NO live LLM call, \emph{even when an
API key is configured}. Set \code{TRUE} to use the LLM for annotation
extraction and semantic enrichment when a key is available; with \code{TRUE}
but no key, the run still degrades silently to deterministic (never an
error). Ignored when \code{supplement} is given (that path makes no live LLM
calls either).}

\item{spec_column_aliases}{Optional named list of extra column-name
aliases for the ADaM spec Excel (see \code{parse_adam_spec()}); useful when
a workbook uses non-standard or non-English headers. Example:
\code{list(variable = "nom de variable", dataset = "domaine")}.}

\item{extract_with_llm}{If \code{TRUE} (default), the LLM re-reads each section's
raw shell cells as the primary annotation reader, separating display label
from variable reference in variant layouts. Every proposed
\code{DATASET.VARIABLE} is gated against the ADaM spec -- out-of-spec proposals
are rejected and logged as blockers, never shipped. This is a sub-control
of the \code{llm} tier: it only has any effect when \code{use_llm = TRUE}. Set
\code{FALSE} to keep the LLM enrichment pass but skip the LLM extraction pass.}

\item{ship_annotations}{If \code{FALSE} (default), programmer annotation lines
found outside the stub cells (e.g. red \code{Label -> DATASET.VAR} paragraphs
below a table) are kept for row binding and the validation report but are
NEVER emitted into the ARS Footnote display section -- rendered footnotes
then contain only true footnotes. Set \code{TRUE} to append them to the
footnotes (debug escape hatch).}

\item{heading_patterns}{Optional character vector of PCRE patterns tried
BEFORE the built-in TLF heading grammars, for sponsor shells whose
headings the built-ins do not recognise. Each pattern must use named
capture groups: \verb{(?<number>...)} (required -- the dotted TLF number),
\verb{(?<type>...)} (optional, matching Table/Figure/Listing; defaults to
Table), and \verb{(?<title>...)} (optional inline title; the title tail is
then decomposed into title/population/source datasets the same way
built-in headings are). Custom patterns are accepted as-is -- the
built-in prose/TOC rejection rules are not applied to them. Not needed
for the built-in formats -- a bare \code{"Table 14.1.1"}, a colon inline
title \code{"Table 14.1.1: Title"}, and one-line headings that carry the
title, a dash-separated population, an inline annotation, and a
programming-datasets suffix together.
Example: \code{"^(?i)Output\\\\s+(?<number>\\\\d+(?:\\\\.\\\\d+)*)\\\\s*:\\\\s*(?<title>.*)$"}.}

\item{validate}{If \code{TRUE} (default), cross-reference annotations
against the ADaM spec and write a validation report.}

\item{report_path}{Path for the validation report \code{.xlsx}. Defaults to
\code{spec_validation_report.xlsx} in \code{\link[=tempdir]{tempdir()}}.}

\item{code_dir}{Directory for the emitted per-TLF pure-\code{{cards}} \code{.R}
deliverables. When \code{NULL} (default) a \verb{code/} folder next to \code{output_path}
is used. These scripts are both the human-readable deliverable and the
engine \code{ars_to_ard()} sources to build the ARD.}

\item{adam_dir}{ADaM directory baked into each emitted script's header
(the reader can edit it). Default \code{"."}.}

\item{verbose}{Print progress messages. Default \code{TRUE}.}
}
\value{
Invisibly returns a named list:
\describe{
\item{\code{ars_path}}{Path to the generated ARS JSON file.}
\item{\code{extraction_mode}}{Which tier ran: \code{"llm"}, \code{"supplement"}, or
\code{"deterministic"}. Also stored in the JSON as
\verb{_meta.extraction_mode}.}
\item{\code{report_path}}{Path to the validation report (if validate=TRUE).}
\item{\code{code_dir}}{Directory holding the emitted per-TLF \code{{cards}} \code{.R}
deliverables.}
\item{\code{code_paths}}{Named character vector of the emitted \code{.R} paths
(names = output ids).}
\item{\code{n_tlfs}}{Number of TLF sections processed.}
\item{\code{n_analyses}}{Number of ARS Analysis objects created.}
\item{\code{n_warnings}}{Number of spec validation warnings.}
\item{\code{reporting_event}}{The full ARS ReportingEvent as a nested R
list -- the same content that was serialised to \code{ars_path}. Inspect
interactively with e.g. \code{str(res$reporting_event, max.level = 2)}.}
\item{\code{validation}}{Data frame of per-annotation validation results
(\code{tlf_number}, \code{stub_label}, \code{annotation}, \code{variable_ref}, \code{status},
\code{message}). \code{NULL} when \code{validate = FALSE}.}
\item{\code{diagnostics}}{Data frame of pipeline diagnostics -- every
fallback, parsing miss, skipped sheet, LLM failure, unknown method,
and dropped where-clause condition recorded during the run
(\code{stage}, \code{severity}, \code{tlf_number}, \code{location}, \code{problem},
\code{action}). Also written to the "Diagnostics" sheet of the
validation report and retrievable via \code{\link[=ars_diagnostics]{ars_diagnostics()}}.}
}
}
\description{
Reads a lead programmer's already-annotated TLF shells Word document and
the study's ADaM specification Excel, and produces a valid CDISC Analysis
Results Standard (ARS) v1.0 ARM-TS JSON file consumable by
\code{siera::readARS()}.
}
\section{Writing identifiable TLF headings}{

arsbridge splits the shell into outputs by finding TLF heading paragraphs.
For a heading to be recognised reliably, write it as \strong{its own ordinary
paragraph} (not inside a text box, shape, table cell, or field code)
that \strong{begins with \code{Table}, \code{Figure}, or \code{Listing} followed by the output
number}. A title should follow the number. All of these are read:

\preformatted{Table 14.1.1
Table 14.1.1: Summary of Demographics
Table 14.1.1 Summary of Demographics
Table 14.1.1 Summary of Demographics - Safety Population ADSL.SAFFL='Y'
Table 14.1.1 Demographics - Screened Subjects ADSL.SCRNFL='Y' [PROGRAMMING DATASETS USED: ADSL]}

The population, an inline annotation, and a
\verb{[PROGRAMMING DATASETS USED: ...]} suffix may all ride on the same line;
annotation values may use single quotes, double quotes, or an unquoted
number (\code{ADSL.COHORTN=1}). The \strong{recommended} form for a clean, portable
shell is the explicit colon title -- \verb{Table 14.1.1: Descriptive Title} --
with the population on the next line.

These are deliberately \strong{not} treated as headings, to avoid false splits:
prose that mentions a number (\verb{Table 14.1.1 shows ...}), cross-references
(\verb{See Table 14.1.1 ...}), table-of-contents lines, and bare section
numbers with no designator (\verb{14.1 Demographic and Baseline Tables}). When
the parser finds no heading, or finds a number but no title, it says so
and repeats this guidance. For a sponsor template whose headings genuinely
differ, pass \code{heading_patterns} rather than reformatting the shell.
}

\section{Human review}{

The generated ARS JSON is a draft. A qualified clinical programmer MUST
review it before downstream use. The JSON includes a
\verb{_meta.requires_human_review = TRUE} field that consumers can key on.
}

\examples{
\dontrun{
spec_to_ars(
  shell_path     = "inputs/annotated_shells.docx",
  adam_spec_path = "inputs/adam_spec.xlsx",
  output_path    = "outputs/reporting_event.json",
  report_path    = "outputs/spec_validation_report.xlsx"
)
}
}
\seealso{
\code{\link[=ars_copilot_instructions]{ars_copilot_instructions()}} and \code{\link[=ars_validate_supplement]{ars_validate_supplement()}} for
the no-API \verb{supplement =} workflow; \code{\link[=set_llm_key]{set_llm_key()}} to configure a live
LLM. Background: \code{vignette("no-api-access")}.
}
