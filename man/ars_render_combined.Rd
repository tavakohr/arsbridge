% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ars_render_docx.R
\name{ars_render_combined}
\alias{ars_render_combined}
\title{Render every output into one combined ARD and one combined RTF}
\usage{
ars_render_combined(
  ars_path,
  file,
  adam_dir = NULL,
  ard = NULL,
  ard_file = NULL,
  output_ids = NULL,
  max_rows = 500
)
}
\arguments{
\item{ars_path}{Path to the ARS reporting-event JSON.}

\item{file}{Path of the combined \code{.rtf} to write.}

\item{adam_dir}{Directory of ADaM datasets. Required to compute the ARD when
\code{ard} is \code{NULL}, and to render listings.}

\item{ard}{Optional precomputed ARD (from \code{\link[=ars_to_ard]{ars_to_ard()}}). When \code{NULL} it is
computed from \code{ars_path} + \code{adam_dir}.}

\item{ard_file}{Optional path to also save the big ARD as an \code{.rds}.}

\item{output_ids}{Optional character vector restricting which outputs to
include (matched against output id or name, case-insensitively).}

\item{max_rows}{Row cap for listings. Default 500.}
}
\value{
Invisibly, a manifest data frame: \code{output_id}, \code{type}, \code{status}
(\code{"rendered"}/\code{"error"}/\code{"skipped"}), \code{reason}. \code{attr(., "file")} is the
RTF path and \code{attr(., "ard_file")} the saved ARD path (or \code{NA}).
}
\description{
The "run all" companion to \code{\link[=ars_render_split]{ars_render_split()}}: computes (or accepts) the
single big ARD covering every analysis, optionally saves it, and writes all
tables and listings into ONE combined RTF file (each table carries its own
id + title header). Figures are not included in an RTF and are reported as
skipped -- use \code{\link[=ars_render_split]{ars_render_split()}} or \code{\link[=ars_render_all]{ars_render_all()}} for those.
}
\seealso{
\code{\link[=ars_render_split]{ars_render_split()}} for one file per program,
\code{\link[=ars_render_all]{ars_render_all()}} for a single combined Word document.
}
