% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_anthropic_key.R
\name{set_anthropic_key}
\alias{set_anthropic_key}
\title{Set (or update) your Anthropic API key for arsbridge}
\usage{
set_anthropic_key(key = NULL, scope = c("user", "project"))
}
\arguments{
\item{key}{Character. Your Anthropic API key (starts with \code{"sk-ant-"}).
If \code{NULL} (default) and R is running interactively, prompts you to
paste the key.}

\item{scope}{\code{"user"} (default) writes to your home \code{.Renviron}
(recommended -- one key shared across all your R projects).
\code{"project"} writes to \code{.Renviron} in the current working directory
(useful when collaborating on a shared project where each contributor
has their own key).}
}
\value{
Invisibly returns the path to the \code{.Renviron} file that was
updated.
}
\description{
Writes \code{ANTHROPIC_API_KEY=...} to your user \code{.Renviron} file so it loads
automatically every time you start R, AND sets it in the current session
so you can call \code{\link[=spec_to_ars]{spec_to_ars()}} immediately -- no restart required.
}
\details{
Get a key at \url{https://console.anthropic.com/settings/keys}.
}
\examples{
\dontrun{
# Interactive prompt (recommended -- key is not echoed to the screen
# when the 'askpass' package is installed)
set_anthropic_key()

# Or paste it directly
set_anthropic_key("sk-ant-api03-...")

# Project-scoped key (writes to ./.Renviron, not your home one)
set_anthropic_key("sk-ant-api03-...", scope = "project")
}
}
