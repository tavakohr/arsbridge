% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ars_model.R
\name{model_to_ars}
\alias{model_to_ars}
\title{Serialize an editable ARS model back to a reporting event}
\usage{
model_to_ars(model, template = NULL)
}
\arguments{
\item{model}{An \code{ars_model} from \code{\link[=ars_to_model]{ars_to_model()}}.}

\item{template}{The reporting event to reassemble from. Defaults to the
template the model was read from, which is what you almost always want.}
}
\value{
A reporting event as a nested list, ready for
\code{jsonlite::toJSON(auto_unbox = TRUE, pretty = TRUE, null = "null")}.
}
\description{
The inverse of \code{\link[=ars_to_model]{ars_to_model()}}. Each pool row's edited fields are written
back into the original node it came from, and the reporting event is
reassembled from the template so that every field the model does not
surface -- including \verb{_meta} and any future ARS key -- survives untouched.
}
\details{
The two tables of contents (\code{mainListOfContents} and
\code{otherListsOfContents}) are pure derivations of the outputs list. They are
copied verbatim when nothing structural changed, and regenerated from the
outputs when analyses or output references were added, removed or
reordered.
}
\examples{
\dontrun{
model <- ars_to_model("reporting_event.json")
model$analyses$methodId[1] <- "MTH_SUBJECT_COUNT"
ars <- model_to_ars(model)
}
}
\seealso{
\code{\link[=ars_to_model]{ars_to_model()}}, \code{\link[=validate_ars_model]{validate_ars_model()}}.
}
