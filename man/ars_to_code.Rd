% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ars_to_code.R
\name{ars_to_code}
\alias{ars_to_code}
\title{Generate the R analysis program for one or more outputs}
\usage{
ars_to_code(
  ars_path,
  output_ids = NULL,
  code_dir,
  overwrite = FALSE,
  subject_key = "USUBJID"
)
}
\arguments{
\item{ars_path}{Path to a saved ARS JSON file.}

\item{output_ids}{Character vector of output ids (or names) to generate.
\code{NULL}, the default, generates every eligible output. An id matching no
output is an error, not an empty result.}

\item{code_dir}{Directory to write the \code{.R} programs into, created if
needed.}

\item{overwrite}{Replace an existing program whose content differs.
Default \code{FALSE}.}

\item{subject_key}{Subject identifier used when resolving analyses.}
}
\value{
Invisibly, a named character vector of the paths written -- names
are output ids, in the order the reporting event lists them.
}
\description{
Writes one self-contained \code{{cards}} \code{.R} program per output (TLF) from an
\strong{already-saved} ARS. This is the explicit generation step: authoring a
reporting event and generating programs from it are two deliberate actions,
so \code{spec_to_ars()} does not do this for you unless you ask it to with
\code{emit_code = TRUE}.
}
\details{
The reporting event is the semantic source of truth. Generation reads the
saved ARS and nothing else -- never the original shell or ADaM
specification -- so a program always reflects the reviewed, approved
document rather than a re-reading of the inputs it came from.
}
\section{What this does not do}{

It generates; it never executes. Nothing here reads data, so no \code{adam_dir}
is needed: the emitted program takes the ADaM location as its own input,
which you set before running it.
}

\section{An existing program is never silently replaced}{

If the target file exists and is byte-identical to what would be written,
nothing happens -- regenerating is safe to repeat. If it exists and
\emph{differs}, generation stops rather than discarding it.

arsbridge deliberately does not claim to know \emph{why} it differs. Emitted
programs carry no provenance, so a file edited by hand and a file generated
from an earlier version of the ARS look exactly alike. Pass
\code{overwrite = TRUE} when you have decided the file on disk is expendable.
}

\examples{
\dontrun{
res <- spec_to_ars_example()          # emit_code = FALSE by default
ars_to_code(res$ars_path, code_dir = "code")            # every output
ars_to_code(res$ars_path, "T_14_1_2", code_dir = "code") # just one
}
}
\seealso{
\code{\link[=spec_to_ars]{spec_to_ars()}} to author the reporting event, \code{\link[=ars_to_ard]{ars_to_ard()}} to
compute results from it, and \code{\link[=edit_ars]{edit_ars()}} to review it first.
}
