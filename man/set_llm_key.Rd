% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_llm_keys.R
\name{set_llm_key}
\alias{set_llm_key}
\title{Set the API key for any supported LLM provider}
\usage{
set_llm_key(provider, key = NULL, scope = c("user", "project"))
}
\arguments{
\item{provider}{Provider id. One of the registry names
(currently \code{"anthropic"}, \code{"openai"}, \code{"gemini"}, \code{"glm"}).}

\item{key}{Character. The API key. If \code{NULL} (default) and R is running
interactively, you are prompted.}

\item{scope}{\code{"user"} (default) targets your home \code{.Renviron};
\code{"project"} targets \code{.Renviron} in the current working directory.}
}
\value{
Invisibly returns the path to the \code{.Renviron} that would be / was
written.
}
\description{
Generic key setter driven by the provider registry. Use this for providers
that have no dedicated \verb{set_*_key()} helper (e.g. \code{"glm"}), or call the
named wrappers (\code{\link[=set_anthropic_key]{set_anthropic_key()}}, \code{\link[=set_openai_key]{set_openai_key()}},
\code{\link[=set_gemini_key]{set_gemini_key()}}) for the common three. Sets the provider's API-key
environment variable for the current session; in an interactive session you
are then asked whether to also persist it to your \code{.Renviron}.
}
\examples{
\dontrun{
# Add a trending OpenAI-compatible provider already in the registry:
set_llm_key("glm", "your-glm-key")
Sys.setenv(ARS_LLM_PROVIDER = "glm")  # make it the active provider
}
}
\seealso{
No API key available? \code{\link[=ars_copilot_instructions]{ars_copilot_instructions()}} sets up the
no-API supplement workflow -- see \code{vignette("no-api-access")}.
}
