% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ars_to_ard.R
\name{ars_manual_worklist}
\alias{ars_manual_worklist}
\title{Manual-derivation worklist from an ARD}
\usage{
ars_manual_worklist(ard)
}
\arguments{
\item{ard}{An ARD data frame of class \code{"card"} from \code{\link[=ars_to_ard]{ars_to_ard()}}.}
}
\value{
A data frame with one row per pending cell: \code{output_id},
\code{analysis_id}, \code{method_id}, \code{group1}, \code{group1_level}, \code{variable},
\code{stat_name}. Zero rows (with those columns) when nothing is pending.
}
\description{
Lists every reserved \code{manual_pending} cell in an ARD produced by
\code{\link[=ars_to_ard]{ars_to_ard()}} -- statistics arsbridge could not compute (a
declared-but-unexecutable method, e.g. a Cochran-Mantel-Haenszel p-value)
and reserved as keyed stub rows. This is the analyst's checklist: each row
must be computed with a validated analysis script and written back into the
ARD (set \code{stat}, \code{result_status = "manual_filled"}, \code{value_source},
\code{derivation_ref}) before the table is final. See \code{vignette("getting-started")}
and the ADRs under \verb{adr/} for the round-trip.
}
\examples{
\dontrun{
  ard <- ars_to_ard("outputs/reporting_event.json", "inputs/ADaM")
  ars_manual_worklist(ard)
}
}
