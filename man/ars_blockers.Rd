% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{ars_blockers}
\alias{ars_blockers}
\title{Blocking problems from the most recent run, in plain English}
\usage{
ars_blockers(diagnostics = ars_diagnostics())
}
\arguments{
\item{diagnostics}{Data frame of diagnostics to summarise. Defaults to the
findings from the most recent run (\code{\link[=ars_diagnostics]{ars_diagnostics()}}).}
}
\value{
Data frame with columns \code{input}, \code{problem}, \code{action}, \code{stage},
\code{tlf_number}, \code{location} -- one row per blocking (FAIL) finding.
}
\description{
The show-stoppers: every \code{FAIL}-severity finding from the most recent
\code{\link[=spec_to_ars]{spec_to_ars()}} or \code{\link[=ars_to_ard]{ars_to_ard()}} call -- the gaps that mean arsbridge
could not produce clean ARS / ARD / ready-to-run R code. Each row names the
input document to open (\code{input}), what is wrong and why (\code{problem}), and how
to fix it (\code{action}). A zero-row result means there were no blocking gaps.
}
\details{
This is the same set surfaced at the top of the validation report
("What to fix first") and returned in the \code{blockers} element of the
\code{\link[=spec_to_ars]{spec_to_ars()}} result; this accessor exists for interactive inspection.
}
\examples{
\dontrun{
spec_to_ars("shells.docx", "adam_spec.xlsx")
ars_blockers()   # what must be fixed, in plain English
}
}
