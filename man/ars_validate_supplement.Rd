% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/supplement.R
\name{ars_validate_supplement}
\alias{ars_validate_supplement}
\title{Validate a Copilot supplement file before running spec_to_ars()}
\usage{
ars_validate_supplement(path, adam_spec_path = NULL)
}
\arguments{
\item{path}{Path to the supplement \code{.json}.}

\item{adam_spec_path}{Optional path to the ADaM spec (\code{.xlsx}/\code{.xml});
enables the spec gate check.}
}
\value{
Invisibly, a data frame of findings with columns \code{severity}
(\code{FAIL}/\code{WARN}/\code{INFO}), \code{tlf}, \code{where}, \code{problem}. Zero rows = clean.
}
\description{
Pre-flight check for the supplement workflow (see
\code{\link[=ars_copilot_instructions]{ars_copilot_instructions()}}): parses the file, checks the format
version, every binding's shape and variable syntax, and the enrichment
enums. With \code{adam_spec_path} it additionally verifies every proposed
variable against the ADaM spec -- the same hard gate \code{spec_to_ars()}
applies. Findings are printed and returned; messages are worded so they
can be pasted back to the assistant to regenerate the file.
}
\examples{
\dontrun{
ars_validate_supplement("supplement.json", "adam_spec.xlsx")
}
}
\seealso{
\code{\link[=ars_copilot_instructions]{ars_copilot_instructions()}} to produce the upload file,
\code{\link[=spec_to_ars]{spec_to_ars()}} to consume the validated supplement (\verb{supplement =}).
Full walkthrough: \code{vignette("no-api-access")}.
}
