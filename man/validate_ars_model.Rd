% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ars_validate.R
\name{validate_ars_model}
\alias{validate_ars_model}
\title{Check an ARS model for integrity, spec and coverage problems}
\usage{
validate_ars_model(model, spec = NULL, report = NULL)
}
\arguments{
\item{model}{An \code{ars_model} from \code{\link[=ars_to_model]{ars_to_model()}}.}

\item{spec}{Optional ADaM spec, as returned by the package's spec reader
(a list with \code{variables} and \code{lookup}). When supplied, datasets and
variables are checked against it.}

\item{report}{Optional annotation validation report -- the data frame
\code{spec_to_ars()} returns as \verb{$validation}, or the "Validation" sheet of the
report it writes. When supplied, annotated shell lines with no
corresponding analysis are reported as gaps.}
}
\value{
A data frame of findings, most severe first, with columns
\code{severity} (\code{"GAP"}, \code{"WARN"} or \code{"INFO"}), \code{entity} (the pool the
finding is about), \code{id}, \code{field}, \code{problem} and \code{action}. Zero rows means
nothing to fix.

\code{GAP} means a specific result will not be produced and the run has
reserved it; \code{WARN} means a number exists but came from a fallback or an
inference, so check it. Reports written before this release carry \code{FAIL}
where they would now carry \code{GAP}, and still sort and render.
}
\description{
Runs the checks that make the review stage guided rather than generic: that
every reference resolves, that every method can actually be executed, that
variables exist in the ADaM spec, and that no annotated shell line was
missed by the generator.
}
\section{What is checked}{

\describe{
\item{Identity}{Every entity has an id, and no id is used twice.}
\item{References}{Every \code{methodId}, \code{analysisSetId}, \code{dataSubsetId} and
grouping id resolves, and every output references analyses that exist.
An empty \code{dataSubsetId} means "no subset" and is not a dangling
reference. Analyses no output displays are reported.}
\item{Executability}{Whether \code{\link[=ars_to_ard]{ars_to_ard()}} can compute each analysis
natively, needs a prerequisite, falls back to the generic summarizer,
or will reserve an empty cell for manual computation.}
\item{Populations}{Analysis sets whose population text could not be
parsed into a condition, and so filter nothing.}
\item{Spec}{With \code{spec}: datasets and variables that are not in the ADaM
spec.}
\item{Result paths}{For an output with declared hierarchical result-group
paths: the path count matches the shell's column count, every shell
column has a path and every path a shell column (no invented
Cartesian combinations), subtotals have a defined scope, no two paths
compose the same condition, and every displayed analysis links all
required grouping factors in header order.}
\item{Coverage}{With \code{report}: shell annotations that no analysis
carries -- lines the generator missed.}
}
}

\examples{
\dontrun{
model <- ars_to_model("reporting_event.json")
findings <- validate_ars_model(model)
subset(findings, severity == "GAP")
}
}
\seealso{
\code{\link[=ars_to_model]{ars_to_model()}}, \code{\link[=model_to_ars]{model_to_ars()}}.
}
