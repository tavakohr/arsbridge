% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_ars_workflow.R
\name{ars_workflow}
\alias{ars_workflow}
\title{Run the guided arsbridge workflow}
\usage{
ars_workflow(project_dir = NULL)
}
\arguments{
\item{project_dir}{Path to the project folder. \code{NULL} (default) works out
which project you meant: the working directory if you are standing in a
project, otherwise the last project opened. Only a genuine first run
starts on an empty setup form. Projects opened before are also offered in
a dropdown, so switching between studies does not mean retyping four
paths; that list is kept in \code{tools::R_user_dir("arsbridge", "config")}
and holds folder paths and nothing else.}
}
\value{
Invisibly, the project directory.
}
\description{
Opens a step-by-step app that walks one study project from an annotated
TLF shell to a reviewed CDISC ARS reporting event:
}
\details{
\enumerate{
\item \strong{Project setup} -- pick a project folder and name the annotated shell
(\code{.docx} or \code{.xlsx}), the ADaM spec (\code{.xlsx}/\code{.xml}), and a study id. The
folder gets a fixed layout (\verb{copilot/}, \verb{ars/}) and a
small \code{arsbridge_project.json} state file. With \code{shinyFiles} installed
each path comes with a Browse button; without it they are text fields,
as before.
\item \strong{Instruction files} -- writes the two-phase Copilot instructions and
the supplement JSON schema into \verb{copilot/} (see
\code{\link[=ars_copilot_instructions]{ars_copilot_instructions()}}).
\item \strong{Phase 1 (manual)} -- upload the Phase 1 instructions, the shell, and
the spec to your chat assistant; paste or upload its
\code{tlf_extraction_blueprints.json} reply back here. A pre-flight check
catches truncated or wrong-version files before they cost a Phase 2
session.
\item \strong{Phase 2 (manual)} -- upload the Phase 2 instructions, the schema,
the shell, and the blueprint; paste or upload \code{supplement.json} back.
\code{\link[=ars_validate_supplement]{ars_validate_supplement()}} runs immediately, and any FAILs come with
a paste-ready repair prompt for the assistant.
\item \strong{Build} -- runs \code{\link[=spec_to_ars]{spec_to_ars()}} with the supplement (or
deterministically when you skip the Copilot phases) and reports the
result: outputs, analyses, warnings, blockers, and the written files.
\item \strong{Review & edit} -- hands the built reporting event to \code{\link[=edit_ars]{edit_ars()}}.
When the editor closes, the workflow reopens with fresh statuses.
}

Every step's status is derived from the files in the project folder, so
closing the app loses nothing: reopening the same \code{project_dir} resumes
exactly where the files stand. Steps 3 and 4 are skippable -- without a
supplement the build runs in deterministic mode.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
ars_workflow()                 # start fresh
ars_workflow("~/my_study")     # resume an existing project
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=spec_to_ars]{spec_to_ars()}}, \code{\link[=ars_copilot_instructions]{ars_copilot_instructions()}},
\code{\link[=ars_validate_supplement]{ars_validate_supplement()}}, \code{\link[=edit_ars]{edit_ars()}}.
}
