% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ars_model.R
\name{ars_to_model}
\alias{ars_to_model}
\title{Turn an ARS reporting event into an editable tabular model}
\usage{
ars_to_model(ars)
}
\arguments{
\item{ars}{Either a path to an ARS JSON file, or an already parsed
reporting event (a list, as returned in \code{spec_to_ars()$reporting_event}).}
}
\value{
An object of class \code{ars_model}: a list with one data frame per
pool -- \code{analyses}, \code{methods}, \code{analysis_sets}, \code{data_subsets},
\code{groupings}, \code{outputs} -- plus \code{template} (the untouched parsed reporting
event) and \code{source_path} (the file it was read from, or \code{NULL}).

Pools that are absent from the reporting event come back as zero-row data
frames with the full column set, so downstream code can always rely on
the columns existing.
}
\description{
Reads a CDISC ARS v1.0 reporting event and returns one data frame per
entity pool, each row carrying the flat fields a reviewer edits plus a
\code{raw} list-column holding the original untouched node. \code{\link[=model_to_ars]{model_to_ars()}}
is the exact inverse: an unedited model round-trips to a structurally
identical reporting event.
}
\details{
This is the foundation of the review/correct stage
(\code{spec_to_ars()} -> review -> \code{\link[=ars_to_ard]{ars_to_ard()}}). It depends only on
\code{jsonlite} -- no Shiny, no LLM -- so the pools are equally usable from a
plain script.
}
\section{Column conventions}{

Columns named exactly like an ARS field (camelCase, e.g. \code{methodId}) map
onto that field one-to-one. Snake_case columns are either derived and
read-only (\code{output_id}, \code{n_analyses}, \code{condition_summary}) or documented
composites that several ARS fields are rebuilt from -- \code{grouping_ids}
(semicolon-separated, ordered, rebuilds \code{orderedGroupings}) and
\code{referenced_analysis_ids} (semicolon-separated, rebuilds the output's
analysis references and therefore the tables of contents).

An \code{NA} in an optional column such as \code{strata} means the key is absent from
the node; setting it back to \code{NA} removes the key again.
}

\examples{
\dontrun{
model <- ars_to_model("reporting_event.json")
model$analyses[, c("id", "methodId", "dataset", "variable")]
}
}
\seealso{
\code{\link[=model_to_ars]{model_to_ars()}} to serialize back, \code{\link[=validate_ars_model]{validate_ars_model()}} to
check integrity.
}
