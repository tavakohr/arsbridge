% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ars_render_figure.R
\name{ars_render_figure}
\alias{ars_render_figure}
\title{Render an ARS figure output to a ggplot}
\usage{
ars_render_figure(
  ars_path,
  adam_dir,
  output_id,
  type = c("auto", "mean_over_time", "responder_over_time", "km", "forest"),
  dataset = NULL,
  value_var = "AVAL",
  time_var = NULL,
  by_var = NULL,
  paramcd = NULL,
  responder_flag = NULL,
  time_event = NULL,
  subject_key = "USUBJID"
)
}
\arguments{
\item{ars_path}{Path to the CDISC ARS JSON.}

\item{adam_dir}{Directory of ADaM datasets (.xpt/.sas7bdat/.csv).}

\item{output_id}{Figure output id or name.}

\item{type}{Figure type; \code{"auto"} infers from the title.}

\item{dataset}{ADaM dataset to plot. Default \code{NULL}: resolved from the
shell's "Source: ..." line carried in the output's \verb{_meta.source_datasets}
(falling back to \code{"ADEFF"} when the shell named no source).}

\item{value_var}{Response value column (default \code{"AVAL"}).}

\item{time_var}{Visit/time column; default auto (\code{AVISITN} then \code{AVISIT}).}

\item{by_var}{Grouping column; default auto (\code{TRT01A} then \code{TRTP}).}

\item{paramcd}{Optional \code{PARAMCD} filter; default inferred from the title.}

\item{responder_flag}{Optional flag column marking responders.}

\item{time_event}{Optional \code{list(time=, event=)} columns for \code{type = "km"}.}

\item{subject_key}{Subject id. Default \code{"USUBJID"}.}
}
\value{
A \code{ggplot} object.
}
\description{
Builds a treatment-group figure from ADaM data for a figure output. Because
the ARS spec does not describe figure analyses, the data mapping is supplied
here; only the title and footnotes are taken from the spec.
}
\details{
Supported \code{type}s:
\itemize{
\item \code{"mean_over_time"} -- mean of \code{value_var} by visit, one line per group.
\item \code{"responder_over_time"} -- percentage of responders by visit, one line per
group (responder = \code{responder_flag == "Y"}, or \code{value_var == 1}).
\item \code{"km"} -- Kaplan-Meier curve (requires a time-to-event dataset; errors with
guidance if absent).
\item \code{"forest"} -- not supported from raw data (needs fitted effect estimates);
errors with guidance.
}
}
\seealso{
\code{\link[=ars_render_tlf]{ars_render_tlf()}}, \code{\link[=ars_render_listing]{ars_render_listing()}}
}
