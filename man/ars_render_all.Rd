% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ars_render_docx.R
\name{ars_render_all}
\alias{ars_render_all}
\title{Render every output of a reporting event into one Word document}
\usage{
ars_render_all(
  ars_path,
  ard,
  adam_dir = NULL,
  file = NULL,
  types = c("table", "listing", "figure"),
  max_rows = 500,
  progress = NULL
)
}
\arguments{
\item{ars_path}{Path to the ARS JSON.}

\item{ard}{Tidy ARD from \code{\link[=ars_to_ard]{ars_to_ard()}} (drives the tables).}

\item{adam_dir}{Directory of ADaM datasets, required to render listings and
figures. If \code{NULL}, those are skipped (recorded in the manifest).}

\item{file}{Output \code{.docx} path. Default: \code{reporting_event_tlfs.docx} in
\code{\link[=tempdir]{tempdir()}}.}

\item{types}{Which output kinds to render. Default all three.}

\item{max_rows}{Row cap for listings (see \code{\link[=ars_render_listing]{ars_render_listing()}}).}

\item{progress}{Optional \verb{function(i, n, output_id)} for progress reporting.}
}
\value{
A data frame manifest (\code{output_id}, \code{type}, \code{status}, \code{reason}),
invisibly carrying the written file path as attribute \code{"file"}.
}
\description{
Walks all outputs of an ARS reporting event, rendering tables with
\code{\link[=ars_render_tlf]{ars_render_tlf()}}, listings with \code{\link[=ars_render_listing]{ars_render_listing()}}, and figures with
\code{\link[=ars_render_figure]{ars_render_figure()}}, and assembles them into a single landscape \code{.docx}
(one output per page). Returns a manifest recording, for every output,
whether it rendered and -- if not -- why.
}
\seealso{
\code{\link[=ars_render_tlf]{ars_render_tlf()}}, \code{\link[=ars_render_listing]{ars_render_listing()}}, \code{\link[=ars_render_figure]{ars_render_figure()}}
}
