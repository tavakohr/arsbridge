% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unresolved_labels.R
\name{ars_unresolved_labels}
\alias{ars_unresolved_labels}
\title{The statistic rows arsbridge refused to bind}
\usage{
ars_unresolved_labels(ars)
}
\arguments{
\item{ars}{The ARS to read, in any of the three shapes a caller has one in:
the path to an ARS \code{.json}, the run result \code{\link[=spec_to_ars]{spec_to_ars()}} returns (the
reporting event is taken from its \code{reporting_event} element), or a
reporting event itself. Anything else is an error rather than an empty
queue -- "nothing is unresolved" and "I was handed the wrong object" must
not look the same, because the first is the answer a caller acts on.}
}
\value{
A data frame, one row per refused shell row, with columns:
\describe{
\item{\code{output_id}}{The ARS output the row belongs to.}
\item{\code{tlf}}{That output's TLF number, as the shell numbered it.}
\item{\code{sheet}}{The worksheet the row is on.}
\item{\code{row}}{The sheet row number.}
\item{\code{label}}{The row label, exactly as authored.}
\item{\code{analysis_id}}{The analysis the row would have been filled from --
its block's parent, or its own when it has one.}
\item{\code{method_id}}{That analysis's ARS method.}
\item{\code{reason}}{\code{"unreadable"} or \code{"unsupported"}, as above.}
\item{\code{tokens}}{List column: the semantic statistics the label asked
for, in reading order. Empty when \code{reason} is \code{"unreadable"}.}
\item{\code{unsupported}}{List column: the subset of \code{tokens} the method does
not declare. Empty when \code{reason} is \code{"unreadable"}.}
\item{\code{available}}{List column: the operations the method DOES declare
-- what the row could have asked for instead.}
}
Zero rows means every statistic row bound. The columns are present either
way, so a caller can bind or filter the result without a special case.

Only a shell built from an \code{.xlsx} carries a cell map, so a reporting
event built from a Word shell always returns zero rows.
}
\description{
A shell row whose label names statistics is filled from the block above it,
and which statistics it gets is decided by what the label says. When that
cannot be established, arsbridge binds \emph{nothing} on the row rather than
binding whichever statistic the method happens to list first -- a
part-bound row shifts its remaining placeholders onto the wrong statistics,
which writes a real number of the wrong thing. The cells stay on their
placeholders and the build stage logs a WARN.
}
\details{
This function returns those same rows as a data frame, so they can be
worked as a queue instead of read as prose.

There are two reasons, and they lead to different work:

\describe{
\item{\code{"unreadable"}}{The label could not be read as a statistic request
at all -- an unfamiliar spelling, or a row that is not a statistic row.
\code{tokens} is empty. Somebody has to say what the row means.}
\item{\code{"unsupported"}}{The label was read correctly, and names a
statistic the row's method does not declare -- a standard-error line
over a method that produces no standard error, say. \code{tokens} holds what
the label asked for and \code{unsupported} holds the part the method cannot
supply. No synonym fixes this: either the row's method is wrong for it,
or the statistic is beyond the engine.}
}
}
\examples{
\dontrun{
built <- spec_to_ars("shell.xlsx", "adam_spec.xlsx")
todo  <- ars_unresolved_labels(built)          # or built$ars_path
todo[todo$reason == "unreadable", c("tlf", "row", "label")]
}
}
\seealso{
\code{\link[=ars_manual_worklist]{ars_manual_worklist()}} for cells reserved because their METHOD is
beyond the engine -- a different queue: those rows are understood and
waiting on a derivation, these are not yet understood.
}
