% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/example_helpers.R
\name{arsbridge_example}
\alias{arsbridge_example}
\title{Bundled training files shipped with arsbridge}
\usage{
arsbridge_example(file = NULL)
}
\arguments{
\item{file}{Character. Name of a bundled file (e.g.
\code{"annotated_shell.docx"}). If \code{NULL} (default), returns a character
vector of every file in the bundle.}
}
\value{
A character path (absolute) when \code{file} is named; a character
vector of file names when \code{file} is \code{NULL}.
}
\description{
Returns the absolute path to a bundled example file, or -- when called
with no argument -- the names of all files available in the bundle.
The bundle is the CDSC-ALZ-201 training study: a synthetic Alzheimer's
disease study built entirely from public material. The datasets are the
CDISC pilot Xanomeline data as packaged by \code{{pharmaverseadam}}
(Apache-2.0, no real patient data), and every file is regenerated by
\code{data-raw/build_example_bundle.R} in the package source.
}
\details{
Files currently in the bundle:
\describe{
\item{\code{annotated_shell.docx}}{Lead-programmer annotated TLF shells for
8 outputs (5 tables, 2 listings, 1 figure), in Word. Uses the
standard red \code{C00000} run convention for ADaM variable
references. Roughly 97 KB.}
\item{\code{annotated_shell.xlsx}}{The Excel counterpart: the same study,
one annotated worksheet per output, in the same order. This is the
shell \code{\link[=ars_fill_shell]{ars_fill_shell()}} can write back filled, so the bundle demos
the whole chain offline. Roughly 15 KB.}
\item{\code{adam_spec.xlsx}}{ADaM specification workbook covering the 5
domains the shells reference (ADSL, ADAE, ADEX, ADCM, ADVS; 123
variables), with labels and types taken from the pharmaverseadam
column metadata, plus codelists for the decoded categories
(SEX, AESEV, ASEV, AESER). Roughly 16 KB.}
\item{\code{ADaM.zip}}{The five pharmaverseadam datasets as XPT files,
restricted to the curated variables; ADVS keeps only
\code{PARAMCD = 'PULSE'}, the one parameter the shells read, and
ADCM only each subject's first occurrence of a medication, which is
what its listing shows. Roughly 217 KB compressed. Unzip and pass the
folder as \code{adam_dir} to \code{\link[=ars_to_ard]{ars_to_ard()}}.}
}
}
\examples{
arsbridge_example()                       # list bundle contents
arsbridge_example("annotated_shell.docx") # path to the Word shell
arsbridge_example("annotated_shell.xlsx") # path to the Excel shell
arsbridge_example("adam_spec.xlsx")       # path to the ADaM spec

}
