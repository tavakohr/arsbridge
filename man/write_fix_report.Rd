% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fix_report.R
\name{write_fix_report}
\alias{write_fix_report}
\title{Write the fix report workbook.}
\usage{
write_fix_report(
  findings,
  output_path,
  reservations = NULL,
  census = NULL,
  diagnostics = NULL,
  run = list()
)
}
\arguments{
\item{findings}{The findings frame from \code{validate_ars_model()} -- or, better,
the gate's own \code{findings}, so the report and the engine describe one set of
defects.}

\item{output_path}{Path of the \code{.xlsx} to write.}

\item{reservations}{The \code{list(by_analysis, by_finding)} map built from
\strong{these} findings. Built from a different set, \code{reserved_as} would
describe reservations that never happened.}

\item{census}{Optionally the \code{census} frame from \code{ars_fill_shell()}, which
turns analysis-level reservations into addressable cells.}

\item{diagnostics}{Optionally a diagnostics frame; its sheet is omitted when
empty.}

\item{run}{A named list of run facts for the Run sheet: \code{extraction_mode},
\code{verdict}, \code{timestamp}, input paths, and counts.}
}
\value{
Invisibly, \code{output_path}.
}
\description{
The per-phase record of what a run could not resolve and where to change it.
Six sheets: the run's own facts, the fix list, the per-code rollup, the
cells that carry no number, the run diagnostics, and the shared legend.
}
