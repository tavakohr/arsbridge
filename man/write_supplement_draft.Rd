% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/supplement_export.R
\name{write_supplement_draft}
\alias{write_supplement_draft}
\title{Write a draft supplement from a parsed shell}
\usage{
write_supplement_draft(
  shell_path,
  output_path = "supplement_draft.json",
  adam_spec_path = NULL,
  heading_patterns = NULL
)
}
\arguments{
\item{shell_path}{Path to the annotated TLF shell (\code{.docx} or \code{.xlsx}).}

\item{output_path}{Where to write the draft JSON.}

\item{adam_spec_path}{Optional path to the ADaM specification workbook.
When given, a draft analysis whose variable is not in the spec is
flagged in \code{reviewItems} (the same gate \code{spec_to_ars()} enforces later).}

\item{heading_patterns}{Optional custom heading patterns, as understood
by the shell parser.}
}
\value{
Invisibly, the normalized output path. The draft is also
validated with \code{\link[=ars_validate_supplement]{ars_validate_supplement()}} and any findings are shown.
}
\description{
Parses an annotated TLF shell and writes what it found as a v4 supplement
JSON: one entry per TLF with the title, population (as a typed
analysis-set condition), and one analysis per annotated stub row. The
file is a \emph{draft}: review it, correct anything the parser got wrong, and
keep the reviewed file under version control as the study's extraction
manifest. Feed it back with
\code{spec_to_ars(supplement = ..., supplement_trust = "prefer_supplement")}
-- a reviewed value then overrides a wrong parse instead of only filling
gaps.
}
\details{
Rows the parser could not annotate, references outside the ADaM spec, and
statistic sub-rows carrying annotations are not silently dropped: each is
listed in the entry's \code{provenance$reviewItems} so the reviewer sees the
full roster of open questions per TLF.
}
