% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ars_to_ard.R
\name{ard_cmh_test}
\alias{ard_cmh_test}
\title{Cochran-Mantel-Haenszel test as an ARD row}
\usage{
ard_cmh_test(data, response, by, strata, correct = TRUE)
}
\arguments{
\item{data}{A data frame.}

\item{response, by, strata}{Length-1 column names of the response variable, the
treatment grouping, and the stratification variable.}

\item{correct}{Logical; request the continuity correction (default \code{TRUE},
honoured only when the response and grouping are both binary).}
}
\value{
A one-row ARD (\code{card}) with the CMH \code{p.value}; \code{stat} is \code{NA} and the
\code{error} column is populated if the test could not be computed.
}
\description{
Stratified CMH chi-square test of association between a response and a
treatment grouping, returned as a single \code{{cards}}-shaped ARD row carrying
the \code{p.value}. arsbridge emits a call to this function for a \code{MTH_CMH_TEST}
analysis (ADR 0001), so the deliverable script and the executed ARD are the
same code. It wraps \code{\link[stats:mantelhaen.test]{stats::mantelhaen.test()}} on the
response x group x strata contingency table -- base R, no extra dependency
(cardx's wrapper is not used). The continuity correction is applied only for
a 2x2xK table.
}
\examples{
\dontrun{
  ard_cmh_test(adeff, response = "AVAL", by = "TRT01A", strata = "REGION")
}
}
\seealso{
\code{\link[=ars_to_ard]{ars_to_ard()}}
}
