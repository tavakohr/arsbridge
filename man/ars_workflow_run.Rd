% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflow_payload.R
\name{ars_workflow_run}
\alias{ars_workflow_run}
\title{Build every output for a study, in one call}
\usage{
ars_workflow_run(
  shell_path,
  adam_spec_path,
  output_dir,
  adam_dir = NULL,
  study_id = "STUDY-001",
  supplement = NULL,
  use_llm = FALSE,
  llm_provider = NULL,
  api_key = NULL,
  derived_dt = NULL,
  log_path = NULL,
  on_progress = NULL
)
}
\arguments{
\item{shell_path}{Annotated TLF shell, \code{.docx} or \code{.xlsx}.}

\item{adam_spec_path}{ADaM spec (\code{.xlsx} or define.xml).}

\item{output_dir}{Directory to write the outputs into; created if absent.}

\item{adam_dir}{Directory of ADaM datasets. Without it the ARD and the
filled workbook are skipped and reported, and only the ARS is built.}

\item{study_id}{Study identifier stamped into the reporting event.}

\item{supplement}{Optional Copilot supplement JSON.}

\item{use_llm}{Whether to call an LLM for the annotations the regex cannot
resolve. \code{FALSE} runs deterministically.}

\item{llm_provider, api_key}{LLM settings, passed explicitly because a
background process inherits neither the option nor the environment.}

\item{derived_dt}{ISO-8601 timestamp to stamp on computed ARD rows. Pass a
fixed value to make a run byte-reproducible; \code{NULL} uses the clock.}

\item{log_path}{Where the run's console output was captured, if the caller
redirected it. Recorded in \code{artifacts$run_log} so the payload can point
at its own log.}

\item{on_progress}{Optional function called with one progress event at a
time: a list with \code{stage}, \code{stage_idx}, \code{n_stages}, \code{i}, \code{n}, \code{label}.
Each stage announces itself with \code{i = 0}, then ticks once per TLF,
analysis, or sheet -- \code{i} counting the items already FINISHED and \code{label}
naming the one now in flight. The work on either side of those loops
(reading the inputs, writing the reporting event, saving the workbook)
ticks as a named step with no count, so no stage runs out silent. Errors
it raises are swallowed -- a progress bar must never take a build down.
\code{NULL} (the default) reports nothing and changes nothing.}
}
\value{
A list with \code{status} (\code{"success"}, \code{"partial"} or \code{"error"}),
\code{timings}, \code{artifacts}, \code{metadata}, \code{diagnostics} (one row per finding,
with \code{severity} -- never split, so \code{INFO} findings survive),
\code{pending} (cells reserved for manual derivation, ADR 0002),
\code{fill} (the fill stage's headline counts -- \code{filled}, \code{pending},
\code{skipped} -- or \code{NULL} when no fill ran),
\code{unfilled_cells} (workbook cells left showing a placeholder, and why),
and \code{error}.
}
\description{
Runs the whole pipeline -- \code{spec_to_ars()}, then \code{ars_to_ard()}, then
\code{ars_fill_shell()} for an Excel shell -- and returns one structured list
describing what it produced. Takes paths rather than data, holds no state,
and never throws, so it can be sent to a background process and its result
rendered by a UI.
}
\details{
This is what \code{ars_workflow()}'s build step calls. It is exported because a
background worker has to be able to reach it (\code{arsbridge::ars_workflow_run}),
and because running the whole pipeline headlessly is useful on its own --
in a script, in a scheduled job, or in a validation run.
}
\examples{
\dontrun{
  payload <- ars_workflow_run(
    shell_path     = "inputs/shells.xlsx",
    adam_spec_path = "inputs/adam_spec.xlsx",
    adam_dir       = "inputs/ADaM",
    output_dir     = "outputs")
  payload$status
  subset(payload$diagnostics, severity == "FAIL")
}
}
\seealso{
\code{\link[=ars_workflow]{ars_workflow()}} for the app, \code{\link[=spec_to_ars]{spec_to_ars()}} for the build step.
}
