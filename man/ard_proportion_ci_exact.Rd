% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ars_to_ard.R
\name{ard_proportion_ci_exact}
\alias{ard_proportion_ci_exact}
\title{Exact (Clopper-Pearson) binomial confidence interval as ARD rows}
\usage{
ard_proportion_ci_exact(data, variables, by = NULL, conf.level = 0.95)
}
\arguments{
\item{data}{A data frame.}

\item{variables}{Length-1 column name of the response variable.}

\item{by}{Optional column name(s) of the grouping (treatment) variable.}

\item{conf.level}{Confidence level (default \code{0.95}).}
}
\value{
A \code{{cards}} ARD of \code{conf.low} / \code{conf.high} rows per group level.
}
\description{
Per-group exact binomial CIs for a categorical response, returned as a
\code{{cards}}-shaped ARD carrying only the interval bounds (\code{conf.low},
\code{conf.high}). arsbridge emits a call to this function for a
\code{MTH_PROPORTION_CI_EXACT} analysis (ADR 0001), so the deliverable script and
the executed ARD are the same code. Wraps
\code{\link[cardx:ard_categorical_ci]{cardx::ard_categorical_ci()}} with \code{method = "clopper-pearson"}; the n / N /
estimate rows cardx also returns are dropped (a paired count analysis
supplies them, and duplicate statistic identities would make
\code{\link[cards:bind_ard]{cards::bind_ard()}} error). The \code{"card"} class is re-asserted after the
subset so the result binds cleanly across \code{{cards}} versions.
}
\examples{
\dontrun{
  ard_proportion_ci_exact(adrs, variables = "AVALC", by = "TRT01A")
}
}
\seealso{
\code{\link[=ars_to_ard]{ars_to_ard()}}
}
