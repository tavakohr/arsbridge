% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ars_conformance.R
\name{ars_conformance}
\alias{ars_conformance}
\title{Check a reporting event against the official CDISC ARS v1.0 schema}
\usage{
ars_conformance(ars, strip_extensions = TRUE, schema_path = NULL)
}
\arguments{
\item{ars}{What to check: a path to an ARS JSON file, a parsed reporting
event, or an \code{ars_model} from \code{\link[=ars_to_model]{ars_to_model()}}.}

\item{strip_extensions}{Strip arsbridge's documented extension fields
before validating (default \code{TRUE}), so the findings show genuine
divergences rather than the extensions the pipeline relies on. Set to
\code{FALSE} to see everything the schema would reject, extensions included.}

\item{schema_path}{Path to an alternative JSON Schema, if you want to
validate against something other than the bundled v1.0.0 export.}
}
\value{
A data frame of schema violations -- \code{where} (the JSON path),
\code{keyword} (the schema rule), \code{problem} -- with zero rows meaning the
event conforms (after stripping, if enabled). The fields that were
stripped are attached as \code{attr(, "stripped_extensions")}.
}
\description{
Validates against the JSON Schema of the CDISC Analysis Results Standard,
pinned to the \code{v1.0.0} release of
\url{https://github.com/cdisc-org/analysis-results-standard} and shipped
with this package -- so the answer does not change when the standard's
development branch does.
}
\details{
This is a different question from \code{\link[=validate_ars_model]{validate_ars_model()}}. That asks
"will this execute, and does every reference resolve?"; this asks "is the
file structurally what the standard says a reporting event is?". A file can
pass either one alone.
}
\section{The sanctioned extensions}{

arsbridge extends ARS v1.0 in documented places: \verb{_meta} blocks (top level
and per output), \code{referencedAnalysisIds} and \code{outputType} on outputs,
display \code{columns}, the nested \code{analysisVariable} duplicate plus
\code{annotation}/\code{sapDescription}/\code{includeTotal}/\code{strata}/\code{variableRole} on
analyses, \code{annotationText} on analysis sets, and \code{supported} on methods.
These exist for siera compatibility, the renderer, and review provenance;
stripping them is what makes the remaining findings meaningful.
}

\section{Known divergences this will report}{

The generator does not yet emit everything ARS v1.0 requires, and this
function reports those gaps rather than hiding them:
\itemize{
\item \code{reason} and \code{purpose} missing on every analysis (required, with
controlled terminology -- assigning them needs a judgement about which
terms apply, so no default is invented);
\item \code{version} emitted as the string \code{"1"} where the standard wants an
integer (top level, outputs and analyses);
\item each entry of \code{displays} written flat, where the standard wraps it
as \verb{OrderedDisplay\{order, display\}} with \code{id} and \code{name} inside;
\item \verb{fileSpecifications[].fileType} emitted as a plain string (e.g.
\code{"rtf"}) where the standard wants a terminology object;
\item the placeholder \code{referencedOperationRole.controlledTerm} of \code{""}
not being one of the allowed terms;
\item \code{name} missing on the contents-list analysis entries.
}
}

\examples{
\dontrun{
findings <- ars_conformance("reporting_event.json")
subset(findings, keyword == "required")
attr(findings, "stripped_extensions")
}
}
\seealso{
\code{\link[=validate_ars_model]{validate_ars_model()}} for referential and executability checks;
\code{\link[=edit_ars]{edit_ars()}} notes the conformance count after each save.
}
