% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ars_to_tfrmt.R
\name{ars_to_tfrmt}
\alias{ars_to_tfrmt}
\title{Build a tfrmt specification for one ARS output}
\usage{
ars_to_tfrmt(
  ars_path,
  ard,
  output_id,
  col_var = NULL,
  label_var = NULL,
  group_vars = NULL
)
}
\arguments{
\item{ars_path}{Path to the CDISC ARS v1.0 JSON (output of \code{\link[=spec_to_ars]{spec_to_ars()}}).}

\item{ard}{Tidy ARD data frame (output of \code{\link[=ars_to_ard]{ars_to_ard()}}).}

\item{output_id}{\code{character(1)} ARS output id or name to render
(case-insensitive).}

\item{col_var, label_var, group_vars}{Optional overrides for the auto-detected
column roles described above.}
}
\value{
A \code{\link[tfrmt:tfrmt]{tfrmt::tfrmt()}} object. Extracted footnotes are attached as the
attribute \code{"arsbridge_footnotes"}; \code{\link[=ars_render_tlf]{ars_render_tlf()}} applies them as GT
source notes.
}
\description{
Translates one output of a CDISC ARS v1.0 reporting event, together with the
tidy ARD produced by \code{\link[=ars_to_ard]{ars_to_ard()}}, into a \code{\link[tfrmt:tfrmt]{tfrmt::tfrmt()}} specification.
The returned object can be rendered with \code{\link[tfrmt:print_to_gt]{tfrmt::print_to_gt()}} or
\code{\link[tfrmt:print_mock_gt]{tfrmt::print_mock_gt()}} -- but the ARD must be flattened and rescaled first
(see \code{\link[=ars_render_tlf]{ars_render_tlf()}}, which does this for you).
}
\details{
Column roles are auto-detected from the \code{{cards}} ARD column names unless
supplied explicitly:
\itemize{
\item \code{col_var} -- the \verb{group*_level} column whose grouping variable is a fixed
(treatment) grouping in the ARS spec.
\item \code{label_var} -- \code{variable_level} when it carries text, else \code{variable}.
\item \code{group_vars} -- remaining \verb{group*_level} columns plus \code{variable} when more
than one analysis variable is present.
}
}
\examples{
\dontrun{
  ars  <- arsbridge_example("reporting_event.json")
  ard  <- ars_to_ard(ars, "inputs/ADaM")
  spec <- ars_to_tfrmt(ars, ard, "T_14_1_1")
}
}
\seealso{
\code{\link[=ars_render_tlf]{ars_render_tlf()}}, \code{\link[=ars_to_tfrmt_list]{ars_to_tfrmt_list()}}
}
