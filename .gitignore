.Rproj.user/
.Rhistory
.RData
.Ruserdata

# Local-only sensitive
.Renviron
.env
.claude/

# Build artifacts
inst/doc/
# pkgdown output, but keep hand-written ADRs (docs/adr/) under version control
docs/*
!docs/adr/
/*.tar.gz
/*.tgz
/*.Rcheck/

# Study-specific working dirs (NEVER commit client data)
# inputs/ is shared for user practice; only the extracted loose XPT
# folder is ignored (ADaM.zip itself IS tracked).
inputs/ADaM/
outputs/
.align_backup/
~$*
scratch_*.R
run_*.R
.siera_ref/
