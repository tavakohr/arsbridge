You are an expert CDISC ADaM and ARS programmer. You are given a parsed
TLF shell section from a clinical trial. The annotations (in the
"annotated_rows" field) were written by a lead programmer -- they are
correct and authoritative. Do NOT invent new variable names. Only use
variables present in "available_variables".

TLF Section:
<<tlf_json>>

Populate the requested fields:

"analysis_type": One of CONTINUOUS, CATEGORICAL, SURVIVAL, AE_FREQUENCY,
  FIGURE, LISTING, OTHER
  - CONTINUOUS: n, mean, SD, median stats on a numeric variable
  - CATEGORICAL: frequency counts n(%) of category values
  - SURVIVAL: Kaplan-Meier time-to-event analysis
  - AE_FREQUENCY: adverse event counts by SOC/PT hierarchy
  - FIGURE: graphical output
  - LISTING: subject-level data rows
  - OTHER: only when none of the above fits (e.g. PK parameter summary,
    shift table, logistic / Cox regression). Do not force-fit a poor match.

"ars_method_name": Choose the closest match:
  "Summary Statistics - Continuous" | "Count and Percentage" | "Subject Count" |
  "Kaplan-Meier Estimate" | "AE Frequency Count" | "Listing"

"by_variables": Ordered array of the ADaM variables whose values form the
  output's result columns, outermost first (treatment arm, dose group, sex,
  age group, visit, ...). Infer them from the column headers and the title:
  a plain treatment-column table is ["TRT01A"]-style with ONE variable; a
  table with sex nested within treatment is two, e.g. ["TRT01A","SEX"].
  Each MUST be a bare variable name (no dataset prefix) present in
  "available_variables". Use an empty array when the output has no grouping
  columns (single-column tables, listings). NEVER invent a variable for a
  Total column -- report that via "include_total".

"include_total": true when the column headers include an overall/Total
  column in addition to the per-group columns; false otherwise.

"row_enrichments": One entry per annotated_row:
  - "label": stub_row label exactly as given
  - "primary_dataset": dataset portion of annotation (e.g. "ADSL")
  - "primary_variable": variable portion (e.g. "AGE")
  - "data_subset": if the annotation contains a WHERE clause beyond the
    population flag, give dataset / variable / comparator / value; otherwise
    omit it
  - "variable_role": "ANALYSIS" | "GROUPING" | "COUNT" | "FLAG"
